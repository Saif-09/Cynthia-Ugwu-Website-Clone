const scroll = new LocomotiveScroll({
    el: document.querySelector('#main'),
    smooth: true
});
//el refers to the element, Element on which locomotive has to be used. In our case we have to use in whole website so I selected #main.